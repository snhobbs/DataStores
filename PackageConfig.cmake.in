# - Config file for the @PACKAGE_NAME@ package
# It defines the following variables
#  @PACKAGE_NAME_UPPER@_INCLUDE_DIRS - include directories for @PACKAGE_NAME@
#  @PACKAGE_NAME_UPPER@_LIBRARIES    - libraries to link against
#  @PACKAGE_NAME_UPPER@_EXECUTABLE   - the bar executable

# Compute paths
get_filename_component("@PACKAGE_NAME_UPPER@_CMAKE_DIR" "${CMAKE_CURRENT_LIST_FILE}" PATH)
set("@PACKAGE_NAME_UPPER@_INCLUDE_DIRS" "@CONF_INCLUDE_DIRS@")
